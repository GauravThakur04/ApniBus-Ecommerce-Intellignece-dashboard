{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py?__route__=$1"
    },
    {
      "source": "/api",
      "destination": "/api/index.py?__route__="
    }
  ]
}
